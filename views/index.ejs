<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My first Prject</title>
  <script src="https://cdn.tailwindcss.com"></script>

</head>

<body>
  <div class="main w-full min-h-screen bg-zinc-900">
    <div class="form p-10">
      <form action="/create" method="post">
        <p class="text-white pt-5">Title</p>
        <input name="title" class="p-3 outline-none bg-zinc-800 w-full rounded-md text-white" type="text" placeholder="The Title goes here">
        <p class="text-white pt-5">description</p>
        <textarea class="p-3 resize-none outline-none bg-zinc-800 w-full rounded-md text-white" name="description" placeholder="The description goes here"></textarea>
        <input class="block text-white bg-blue-500 rounded-md p-3 mt-3" type="submit">
      </form>
    </div>
    <div class="tasks p-10 flex gap-3 flex-wrap">
      <% if (files.length > 0) {%>
      <% files.forEach(function(val){%>
      <div class="task min-w-72 bg-zinc-800 px-3 py-4 rounded-md inline-block">
        <h1 class="text-white text-3xl tracking-tighter "><%= val %></h1>
        <div class="justify-between items-center mt-3 flex w-full">
          <a class="text-blue-500 " href="/file/<%= val %>">Read more...</a>
          <a class="text-zinc-600 " href="/edit/<%= val %>">edit</a>
          <a class="text-red-600" href="/delete/<%= val %>">delete</a>
        </div>
      </div>
      <%}) %>
      <%} else {%>
      <h1 class="text-zinc-600">No Tasks yet.</h1>
      <% } %>

    </div>
  </div>
</body>

</html>